# ============================================
# OnliOps - Coolify Environment Variables
# ============================================
# Copy these to Coolify's Environment tab
# ============================================

# ===========================================
# DATABASE (Required - managed by docker-compose)
# ===========================================
# PostgreSQL credentials (internal Docker network)
POSTGRES_USER=onliops
POSTGRES_PASSWORD=YourSecurePassword123!
POSTGRES_DB=onliops

# ===========================================
# SUPABASE AUTH (Optional - if using Supabase for auth)
# ===========================================
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# ===========================================
# AI SERVICES (Optional - for AI features)
# ===========================================
# Choose one or more:

# OpenAI (recommended for best results)
# OPENAI_API_KEY=sk-your-openai-key

# Groq (fast and cheap)
# GROQ_API_KEY=gsk_your-groq-key

# OpenRouter (access to multiple models)
# OPENROUTER_API_KEY=sk-or-your-openrouter-key

# Ollama (self-hosted, requires separate container)
# OLLAMA_URL=http://ollama:11434

# ===========================================
# ⚠️  IMPORTANT NOTES
# ===========================================
# 1. Change POSTGRES_PASSWORD to a strong password!
# 2. Database is created automatically on first run
# 3. Migrations are applied from supabase/migrations/
# 4. Data is persisted in Docker volume "onliops-postgres-data"
